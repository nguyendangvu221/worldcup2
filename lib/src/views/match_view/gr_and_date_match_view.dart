import 'package:flutter/material.dart';

class GroupAndDateMatch extends StatelessWidget {
  const GroupAndDateMatch({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Text("group")
      ],
    );
  }
}